# 项目变更记录

本文件用于持续记录项目实际发生的新增、修复和删除，方便后续接手、回归与追踪。

## 记录规则

1. 一次只实施一个小批次；当前批次验证通过后，再开始下一批。
2. “变更记录”只写已经实际完成的内容；尚未实施的工作写入“分批计划”。
3. 每项变更必须写明类型、影响范围和真实验证结果，不使用“应该通过”等推测性表述。
4. 删除数据、文件、接口或功能时，必须记录精确目标、影响、备份及恢复方式；破坏性操作仍需事先获得用户确认。
5. 新增依赖必须单独记录名称、版本、用途和许可证，并遵守项目的事前确认要求。
6. 不记录密钥、密码、个人数据或完整本机敏感路径。

状态统一使用：`待实施`、`进行中`、`已完成`、`受阻`、`已取消`。

## 变更记录

| 编号 | 日期 | 批次 | 类型 | 模块 | 实际变更 | 影响文件或数据 | 验证结果 | 状态 | 备注/恢复方式 |
|---|---|---|---|---|---|---|---|---|---|
| CHG-0001 | 2026-09-13 | B00 | 新增 | 项目文档 | 建立统一变更记录和分批实施计划 | `docs/变更记录.md` | 已检查文件创建成功 | 已完成 | 仅新增文档，无业务代码和数据变化；删除本文件即可恢复 |
| CHG-0002 | 2026-09-13 | B00 | 文档 | 协作规范 | 在根协作说明中增加变更记录入口和单批次执行约束 | `AGENTS.md` | 已检查规则入口存在 | 已完成 | 仅调整协作文档；删除对应规则行即可恢复 |
| CHG-0003 | 2026-09-13 | EXT-01 | 新增 | 数据库 | 提交 `f72c308` 新增资料元数据、资料标签、知识库别名三张表 | `V7__document_metadata.sql`；`document_metadata`、`document_tags`、`library_aliases` | 后端全量测试 `138/138` 通过 | 已完成 | 未新增依赖；迁移已进入主分支，不执行回退或删表 |
| CHG-0004 | 2026-09-13 | EXT-01 | 新增 | 资料管理 | 增加资料标题、生命周期、标签和人工校对接口及服务逻辑 | `DocumentController.java`、`DocumentService.java` | 后端全量测试 `138/138` 通过 | 已完成 | 审查发现的边界问题列入 `R01`、`R02`，本行只记录已落地事实 |
| CHG-0005 | 2026-09-13 | EXT-01 | 新增 | 知识库 | 增加知识库别名读写，并在知识库编辑界面支持维护别名 | `LibraryController.java`、`LibraryService.java`、`LibraryDetail.java`、`libraryApi.ts`、`LibraryView.vue` | 后端测试、前端 type-check 与 build 通过 | 已完成 | 别名与库基本信息分两次请求的原子性问题列入后续观察 |
| CHG-0006 | 2026-09-13 | EXT-01 | 调整 | AI 自动整理与复审 | 自动整理时读取/沉淀知识库别名并写入资料 AI 标签；AI 复审完成后汇总可信度信息 | `LibraryOrganizationService.java`、`AiReviewService.java` | 后端全量测试 `138/138` 通过 | 已完成 | 超长主题名写标签的风险列入 `R01` |
| CHG-0007 | 2026-09-13 | EXT-01 | 新增 | 前端 | 知识库资料列表增加属性面板、生命周期、可信度徽标和标签 chips | `types.ts`、`ingestionApi.ts`、`LibraryView.vue` | `npm.cmd run type-check`、`npm.cmd run build` 均通过 | 已完成 | 构建仅有既有的大 chunk 警告；时间和哈希文案准确性列入 `R02` |
| CHG-0008 | 2026-09-13 | EXT-01 | 新增 | 测试 | 增加资料属性、标签、知识库别名与自动整理联动集成测试，并扩展 AI 复审测试 | `DocumentMetadataFlowIntegrationTest.java`、`AiReviewFlowIntegrationTest.java` | 本机复跑共 `138` 项测试，失败 `0`、错误 `0` | 已完成 | 项目说明原写 `146`，已按本次真实执行结果纠正 |
| CHG-0009 | 2026-09-13 | EXT-01 | 删除 | 全项目 | 本次提交没有删除文件、接口、数据表、功能或依赖 | 提交 `f72c308`（16 个文件：新增 3、修改 13） | 已核对 `git diff HEAD^ --name-status` 和依赖清单 | 已完成 | 代码行减少属于原文件替换，不作为功能删除记录 |
| CHG-0010 | 2026-09-13 | R01 | 修复 | 资料接口 | 对资料属性请求启用 Bean Validation，缺失 `humanChecked` 或空标签稳定返回 400 | `DocumentController.java` | `DocumentControllerTest` 相关 3 项通过；全量测试通过 | 已完成 | 没有改变合法请求的响应合同 |
| CHG-0011 | 2026-09-13 | R01 | 修复 | 资料标签 | 清洗控制字符后重新判空；标签满额时允许同名幂等/AI 转人工；新增查询参数删除入口并保留旧路由；AI 标签受 100 字和 20 个上限保护 | `DocumentService.java`、`LibraryOrganizationService.java`、`ingestionApi.ts` | 定向测试 `11/11`、后端全量 `143/143`、前端 build 通过 | 已完成 | 无数据库迁移、无新依赖、无接口删除；旧删除路由仍兼容 |
| CHG-0012 | 2026-09-13 | R01 | 新增 | 测试 | 增加请求校验、斜杠标签、标签满额幂等和超长 AI 标签回归覆盖 | `DocumentControllerTest.java`、`DocumentMetadataFlowIntegrationTest.java` | 新增 5 项测试；全量失败 `0`、错误 `0` | 已完成 | 集成夹具测试后自动清理，无业务数据变更 |
| CHG-0013 | 2026-09-13 | R01 | 文档 | 登记外部提交审查结果、审查发现和分批修复边界，并纠正历史测试数量 | `docs/变更记录.md`、`AGENTS.md` | 已核对记录与本机命令结果一致 | 已完成 | 下一批为 `R02`，本批未提前实施 |
| CHG-0014 | 2026-09-13 | HANDOFF | 新增 | 项目文档 | 生成可整段交给其他 AI 的 R02 实施提示词，包含范围、现状保护、验收和记录要求 | `docs/交接提示词-R02-资料属性准确性修复.md` | 已检查文件内容与当前 `R02`、`R01` 状态一致 | 已完成 | 仅新增交接文档；删除该文件即可恢复，不影响业务代码 |
| CHG-0015 | 2026-09-13 | R02 | 修复 | 资料接口 | `DocumentView` 新增可空 `updatedAt`（取自 `document_metadata.updated_at`），列表与详情查询真实返回；无元数据的存量资料返回 null | `DocumentService.java` | 定向测试含可控旧时间夹具验证更新后时间确实推进 | 已完成 | 不再用 `created_at` 冒充元数据更新时间；未新增迁移 |
| CHG-0016 | 2026-09-13 | R02 | 修复 | 资料可信度 | 上传时哈希对象改为 `{sha256, recordedAt}` 记录语义，不再写入 `matched`/`checkedAt`；界面徽标改为「哈希已记录」，提示「上传时计算并记录 SHA-256」 | `DocumentService.java`、`types.ts`、`LibraryView.vue`、`AGENTS.md` | 定向测试断言新数据无 `matched`/`checkedAt` 且含 `recordedAt` | 已完成 | 存量 JSON 未批量改写，前端类型兼容旧字段；无迁移 |
| CHG-0017 | 2026-09-13 | R02 | 调整 | 资料可信度 | 增加后端回归测试，确认既有接口可保存 `humanChecked=false`；前端双向提交由 `R02A`（CHG-0022）收尾闭环 | `DocumentMetadataFlowIntegrationTest.java`、`LibraryView.vue` | 后端测试通过；`R02A` 完成后 UI 实测取消/重勾均正确落库 | 已完成 | 前端缺口已由 `R02A` 修复 |
| CHG-0018 | 2026-09-13 | R02 | 新增 | 测试 | 新增元数据 updatedAt 推进、后端人工校对 false 保存、上传哈希记录语义三项后端回归覆盖 | `DocumentMetadataFlowIntegrationTest.java` | 定向 `11/11`；后端全量 `146/146` 失败 0 错误 0 | 已完成 | 后端测试不覆盖前端是否实际发送 false；使用可控旧时间夹具，无 sleep 依赖 |
| CHG-0019 | 2026-09-13 | R02 | 删除 | 全项目 | R02 没有删除文件、接口、数据表、业务数据、功能或依赖 | 当前工作区 R02 差异 | 已核对 `git diff --name-status`、依赖清单和迁移目录 | 已完成 | 无需恢复；本行用于明确记录“无删除” |
| CHG-0020 | 2026-09-13 | AUDIT-02 | 文档 | 审查 | 复核 R01/R02 未提交差异和验证结果，发现人工校对取消未落地、可空类型合同不一致及保存期间取消的空引用风险，并纠正批次状态 | `docs/变更记录.md` | 后端 `146/146`；前端 build（含 type-check）通过；`git diff --check` 通过 | 已完成 | 测试全绿不等于前端交互闭环；下一批改为 `R02A` |
| CHG-0021 | 2026-09-13 | HANDOFF | 新增 | 项目文档 | 生成可整段交给其他 AI 的 R02A 收尾提示词 | `docs/交接提示词-R02A-人工校对取消闭环.md` | 已按本次审查证据、执行边界和验收条件核对 | 已完成 | 仅新增交接文档；删除该文件即可恢复，不影响业务代码 |
| CHG-0022 | 2026-09-13 | R02A | 修复 | 前端资料编辑 | `saveDocEditor` 进入即快照编辑对象，`await` 后不再解引用 `docEditor.value`；人工校对按「当前值 ≠ 原值」双向提交（取消时省略 note）；仅当仍在编辑同一资料时关闭弹窗与提示错误 | `LibraryView.vue` | 源码逐行核对 true/false 两条路径；UI 实测取消→API `checked=false`、重勾→`checked=true` | 已完成 | 修复空引用风险与误关弹窗问题；未改后端接口 |
| CHG-0023 | 2026-09-13 | R02A | 修复 | 前端类型 | `DocumentDto.updatedAt` 对齐后端可空响应为 `string \| null`，界面保留「更新/创建」区分逻辑 | `types.ts` | `npm run type-check`、`npm run build` 通过 | 已完成 | 无删除；兼容存量资料 null 值 |
| CHG-0024 | 2026-09-13 | R02A | 新增 | 验证 | 后端全量回归确认 R02/R02A 未破坏既有成果 | 后端 `mvnw test`；前端 `npm run type-check`、`npm run build`；`git diff --check` | 后端 `146/146` 失败 0 错误 0；前端全部通过；diff 检查通过 | 已完成 | 无删除；无新增依赖与迁移 |
| CHG-0025 | 2026-09-13 | R02B | 修复 | 前端资料编辑 | 资料保存一致性：关闭弹窗与错误写入改用编辑对象身份判断（旧请求不得影响重开的同资料弹窗）；元数据成功后立即同步列表，人工校对成功后二次同步；人工校对失败时保留弹窗与勾选值，明确提示「标题和生命周期已保存，但人工校对状态保存失败」 | `LibraryView.vue` | `npm run type-check`、`npm run build` 通过；`git diff --check` 通过；浏览器实测正常保存路径列表立即更新且弹窗关闭；部分失败路径未手工构造（需令单个请求失败，不修改后端无法可靠触发），已按源码逐行核对 | 已完成 | 纯前端局部控制流；无新增依赖、迁移、后端修改与删除 |
| CHG-0026 | 2026-09-13 | GR01 | 修复 | 图谱工作台 | 模式/深度切换的目标节点规则改为「选中节点优先、子图中心兜底」：`setMode` 非 global 与 `setDepth` 均使用 `selectedNodeId ?? centerNodeId`，两者皆无时保持当前有效视图（不再切入无中心的伪局部/伪聚焦）；邻域加载成功后将 `selectedNodeId` 对齐本次实际中心，消除上次子图的选中残留 | `useGraphWorkspace.ts` | `npm run type-check`、`npm run build` 通过；`git diff --check` 通过；浏览器实测：选中链接表(320)后局部→`node=320&depth=2&mode=local`、聚焦→`node=320&depth=1&mode=focus`，中心卡片与 AI 上下文均为链接表；选中双向链表(309)后深度 1↔2 均以 309 为中心；无选中时聚焦兜底当前中心 309；全局/重置回总览且选中清空；2D/3D 切换保持当前视图；顶部搜索 TCP 跳转 `node=854` 正常 | 已完成 | 仅改 `useGraphWorkspace.ts`；无新增依赖、无删除、无后端/数据库/业务数据修改；单击选中仍不触发网络请求 |
| CHG-0027 | 2026-09-13 | GR02 | 调整 | 图谱工作台 | 模式语义收敛为固定映射：局部恒为 `depth=2`、聚焦恒为 `depth=1`；`setMode` 按 `next === 'local' ? 2 : 1` 生成 URL 并继续使用「选中优先、中心兜底」；`applyRouteQuery` 按 mode 规范化旧 URL（`local&depth=1`→`depth=2`、`focus&depth=2`→`depth=1`、缺 depth 补模式固定值、非法 mode 按局部处理），规范化经 `router.replace` 交由路由 watcher 重载（实测各自仅发起 1 次邻域请求）；`focusFromSearch` 去掉可变深度参数固定聚焦一层；搜索联想、AI 补全、知识库节点、洞察节点四类跳转入口统一为 `focus&depth=1` | `useGraphWorkspace.ts`、`SearchBar.vue`、`LibraryView.vue`、`InsightsView.vue` | `rg` 静态扫描无残留 `local+depth1` 入口/`setDepth`/`:depth=`/`update:depth`；type-check、build 通过；浏览器实测用例 A/B/C/D/E 全部通过 | 已完成 | 覆盖旧文档中局部深度 1/2 可切换的旧要求；无新增依赖与后端修改 |
| CHG-0028 | 2026-09-13 | GR02 | 修复 | 图谱工作台 | 在固定深度语义下保留并复验 GR01 中心同步：邻域加载成功后 `selectedNodeId` 对齐实际中心（`focusNode` 成功分支），`setMode` 继续「选中优先、中心兜底」，多次模式切换不残留旧选中、不跳回旧中心 | `useGraphWorkspace.ts` | 浏览器复测：选中链接表(320)切局部/聚焦均以 320 为中心；选中双向链表(309)后聚焦以 309 为中心；聚焦↔局部互切中心不变 | 已完成 | GR01 修复未被本批回退 |
| CHG-0029 | 2026-09-13 | GR02 | 删除 | 图谱工作台 | 删除工具栏「深度 1/2」选择控件及只为手动深度服务的代码：`GraphToolbar` 深度 UI、`depth` prop、`update:depth` emit；`GraphView` 对应绑定与解构；`useGraphWorkspace` 的 `setDepth()`、可变 `depth` ref 及其导出（`focusNode` 保留显式深度参数，仅由模式映射或双击扩展提供） | `GraphToolbar.vue`、`GraphView.vue`、`useGraphWorkspace.ts` | type-check/build 通过；工具栏实测仅剩全局/局部/聚焦/2D/3D/重置/AI 助手 | 已完成 | 未删除文件、接口、业务数据或测试；无文件级删除 |
| CHG-0030 | 2026-09-13 | GR02 | 文档 | 项目文档 | 同步与新模式语义直接冲突的旧描述：`AGENTS.md` 搜索/AI 补全/快速自检示例的跳转 URL 改为 `mode=focus`；开发文档搜索命中 URL 改为 `focus&depth=1`、「局部搜索默认深度 1，用户可扩展到深度 2」改为「局部固定 2 层、聚焦固定 1 层，工具栏不提供深度切换」 | `AGENTS.md`、`docs/第一版开发文档-Java-TailAdmin.md` | `rg` 复查无 `mode=local&depth=1` 残留描述 | 已完成 | 仅改直接冲突文字，未重写历史 |
| CHG-0031 | 2026-09-13 | SEC01 | 修复 | 图谱 3D 展示 | 3D 图谱 Tooltip 存储型 XSS：`nodeLabelHtml`/`linkLabelHtml` 直接以字符串插值渲染节点名、类型标签、定义与关系名，而底层 float-tooltip 通过 `tooltipEl.html(content)` 写入 DOM；现新增局部 `escapeHtml`（编码 `& < > " '`，入参放宽为 unknown 以覆盖数字关联度），四类不可信字段全部转义，定义截断保持在转义之前 | `GraphCanvas3D.vue`（1 文件，+21/−4） | `npx eslint src/features/graph/GraphCanvas3D.vue` 通过；`npm run type-check`、`npm run build` 通过；从真实组件源码抽取三函数以 XSS 载荷执行 7 项断言全部通过；浏览器渲染边界对照：修复前生成 1 个 img 且 `onerror` 执行，修复后 img/script 均为 0 且脚本未执行、显示为纯文本 | 已完成 | 本批无删除（未删 Tooltip、接口、文件或样式）；未新增依赖；临时测试节点与关系已删除；构建仅保留既有大 chunk 警告 |
| CHG-0032 | 2026-09-14 | CHAT01 | 调整 | 图谱工作台 | AI 知识助手桌面端默认折叠；状态改为 AiChatPanel 实例局部（删除模块级 states Map），关闭/重开、刷新、离开页面后消息为空 | `GraphView.vue`、`useAiChat.ts`、`AiChatPanel.vue` | 浏览器实测：默认折叠、重开为空、刷新为空 | 已完成 | 无新增依赖；不写 localStorage/sessionStorage |
| CHG-0033 | 2026-09-14 | CHAT01 | 新增 | 后端问答 | 新增无状态问答接口 `POST /api/nodes/{nodeId}/chat`：content 必填限长、depth 仅 1/2、history 可空最多 10 条且 role 仅 user/assistant；流程为 图谱上下文 → 防注入 system → 临时历史 → 当前问题 → 模型 | `ChatController.java`、`ChatService.java`、`chatApi.ts` | ChatControllerTest 7 项、ChatServiceAskTest 10 项通过；后端全量 152/152 | 已完成 | 全程无数据库写入；无迁移 |
| CHG-0034 | 2026-09-14 | CHAT01 | 删除 | 后端问答 | 移除 `GET/POST /api/nodes/{nodeId}/chat/sessions`、`GET/POST /api/chat/sessions/{sessionId}/messages`、`DELETE /api/chat/sessions/{sessionId}` 五条路由，以及 ChatService 中 JdbcClient/ObjectMapper/会话增删查与消息持久化代码 | `ChatController.java`、`ChatService.java` | 浏览器与 curl 验证旧路由返回 404；测试断言控制器仅存一条无状态路由 | 已完成 | `chat_sessions`/`chat_messages` 表与既有数据保留、未删除；未建新表 |
| CHG-0035 | 2026-09-14 | CHAT01 | 修复 | 前端问答 | 切换节点立即清空旧消息并中断在途请求；关闭面板（组件卸载）中止全部在途请求并清空内存，慢响应不串入新面板；本地错误提示与「已停止」提示不计入临时历史 | `useAiChat.ts`、`AiChatPanel.vue` | 浏览器实测：在途 6 秒延迟响应在关闭/切换后被丢弃，面板保持空态 | 已完成 | 保留 LLM_* 中文错误映射与停止生成 |
| CHG-0036 | 2026-09-14 | CHAT01 | 新增 | 测试 | 新增与改写后端回归：无状态路由包络、空问题 400、非法 depth/history 400、消息顺序 system→history→当前问题、证据不足透传、无数据库依赖（反射断言）等 | `ChatControllerTest.java`、`ChatServiceAskTest.java` | 定向 22/22；后端全量 152/152 失败 0 错误 0 | 已完成 | 未新增依赖；未跳过任何测试 |
| CHG-0037 | 2026-09-14 | CHAT01 | 文档 | 项目文档 | 记录最新无历史决策：开发文档 §7.1/§7.2/§11.3/§12.6/阶段 F/§17.5 与根 `AGENTS.md` 同步；历史表标注「为兼容保留，当前问答流程不读写」 | `docs/第一版开发文档-Java-TailAdmin.md`、`AGENTS.md`、`docs/变更记录.md` | 已核对文档条文与新接口一致 | 已完成 | 未删除建表定义与历史数据 |
| CHG-0038 | 2026-09-14 | LIB01 | 修复 | 后端测试 | `DocumentMetadataFlowIntegrationTest` 夹具改为测试事务自动回滚：类上加 `@Transactional`，删除 `createdLibraryIds` 字段与其 add 调用、以及数据库 `@AfterEach` 手工清理；组织测试新建库随事务回滚；上传用例保留 `finally`（只删磁盘文件，数据库记录交给回滚） | `DocumentMetadataFlowIntegrationTest.java` | 连续两次定向 `11/11` 通过；两次运行后业务库列表未新增任何测试库；后端全量 `152/152` 失败 0 错误 0 | 已完成 | 未改业务断言、未降低覆盖、未引入新依赖；中断测试也不再残留夹具 |
| CHG-0039 | 2026-09-14 | LIB01 | 修复 | 知识库界面 | 知识库卡片「资料」与「查看知识」改为整页全局互斥：打开任一面板前先关闭另一面板并清理其 loading/error/临时数组；再次点击当前展开按钮仍为收起；保留 aria-expanded/aria-controls/按钮文案与加载错误态 | `LibraryView.vue` | 浏览器 A–F 六组用例全部符合预期；`npx eslint` 单文件、`npm run type-check`、`npm run build` 通过 | 已完成 | 最小改动，未重构页面、未引入状态库或新组件 |
| CHG-0040 | 2026-09-14 | LIB01 | 删除 | 测试业务数据 | 逐条核验（ID+名称完全一致、documentCount/nodeCount/edgeCount 均为 0、名称符合测试生成模式）后，通过 `DELETE /api/libraries/{id}` 逐个删除 7 个残留空测试库：382 元数据测试库88658184569800（0/0/0）、383 元数据测试库88658601827600（0/0/0）、384 元数据测试库88658630658300（0/0/0）、385 元数据测试库88658658310900（0/0/0）、386 元数据测试库88658694936900（0/0/0）、387 元数据测试库88658716586900（0/0/0）、389 堆栈专题88698999265900（0/0/0） | `knowledge_libraries` 中上述 7 行 | 删除后 `GET /api/libraries` 返回 9 个库，7 个残留全部消失，9 个正常知识库（Java语言基础/数据结构与算法基础/线性表/栈与队列/递归与分治/树与二叉树/图/查找/排序）完整无缺失 | 已完成 | 未使用 LIKE/REGEXP/范围 SQL；未删除正常知识库与业务数据；删除对象均为空测试库 |
| CHG-0041 | 2026-09-14 | LIB01 | 文档 | 验证记录 | 登记本批真实执行结果：两次定向测试、后端全量测试、前端 lint/type-check/build、六组浏览器互斥用例、`git diff --check`；并补登 CHAT01 计划行（此前登记遗漏，仅补计划行不改历史 CHG） | `docs/变更记录.md` | 定向 `11/11`×2；全量 `152/152`；eslint/type-check/build 通过；浏览器 A–F 通过；`git diff --check` 通过 | 已完成 | 未把未执行内容写入；未顺手实施其他审查项 |
| CHG-0042 | 2026-09-14 | ZIP01 | 修复 | 资料摄取 | ZIP 内的 PDF 条目曾被判为非法并整包拒绝（用户上传 textbook.zip 报「ZIP 仅支持 JPG/JPEG/PNG/WebP 图片，发现非法条目：…dsacpp-3rd-edn.pdf」）。现放行 PDF 条目：`ZipSafety` 允许 `pdf` 并更名 `ExtractedEntry`，`ZipImageExtractor` 注入 `PdfTextExtractor` 将 PDF 按页生成 page 单元（定位为「ZIP 内文件名 + 第 N 页」，证据可追溯），图片条目仍走视觉 OCR 通道；提示文案同步为「可放图片或 PDF / 最多 500 个条目」 | `ZipSafety.java`、`ZipImageExtractor.java`、`ImportDocumentModal.vue` | 定向 `13/13`（ZipSafety 6 + ZipImageExtractor 2 + DocumentValidator 5）；全量 `183/183`；端到端：含 PNG+PDF 的 ZIP 上传 200（此前 415），单元为 image×1 + page×2（`textbook/dsacpp-3rd-edn.pdf 第 1/2 页`，文本已提取）；前端 eslint/type-check/build 通过 | 已完成 | 仍拒绝 docx/rar 等其它类型与路径穿越；测试用文档与候选已清理，未入库节点；无新增依赖、无迁移 |
| CHG-0043 | 2026-09-14 | ZIP01 | 文档 | 验证记录 | 登记本批真实结果：两次定向/全量测试数量、ZIP 端到端上传与单元构成、前端 lint/type-check/build、`git diff --check`；顺手清理 `ImportDocumentModal.vue` 既有的未使用 `catch (error)` 绑定（该文件此前 lint 失败，非本批引入） | `docs/变更记录.md` | 全量 `183/183`；`git diff --check` 通过 | 已完成 | 未把未执行内容写入 |
| CHG-0042 | 2026-09-14 | RAG01 | 新增 | 原文检索 | 新增只读 `RetrievalService`：检索已完成、生命周期为在用的 `document_chunks`；显式问题以问题词检索全文并为当前节点所属库及共享归属资料加权，代词/承接式问题才回退 `node_evidence`；去重、最多 8 段/8000 字，不写库、不重跑解析 | `RetrievalService.java` | 关键词/代词解析单测 3 项；后端全量 `156/156` 失败 0 错误 0 | 已完成 | 无迁移、无新依赖；归档/过期资料不进入问答上下文 |
| CHG-0043 | 2026-09-14 | RAG01 | 修复 | AI 问答 | `ChatService` 将图谱导航上下文与原文分段上下文分开提供给模型；原文事实强制用 `[编号]` 标注，服务端只将回答中有效编号映射成真实资料、单元、页码与原文摘录；证据不足时不返回引用 | `ChatService.java`、`ChatServiceAskTest.java`、`ChatServiceContextTest.java` | 问答定向 `26/26`；新增真实引用/原文上下文断言；后端全量 `156/156` | 已完成 | 保持 CHAT01 无历史临时对话；ChatService 不直接持有数据库依赖，检索层仅执行 SELECT |
| CHG-0044 | 2026-09-14 | ZIP02 | 修复 | 资料抽取 | 整本教材级资料被批次上限硬拒（用户任务 #938：ZIP 内教材 1250 单元 / 2427 片段 / 342 万字，按 6000 字/批需约 570 批，超过原硬编码上限 60 → 413 PAYLOAD_TOO_LARGE）。现：单批预算 6000→12000 字、单批片段 8→16 条；批次上限改为可配置 `kg.extraction.max-batches`（默认 400，可用环境变量 KG_EXTRACTION_MAX_BATCHES 覆盖）；超限错误改为给出实际需求批次数与可调配置项 | `ExtractionService.java`、`application.yml` | 定向 `ExtractionBatchLimitTest` 3/3；后端全量 `186/186` 失败 0 错误 0 | 已完成 | 未新增依赖、无迁移；批次上限仍是成本护栏（调低可限制单次模型调用量） |
| CHG-0045 | 2026-09-14 | ZIP02 | 测试 | 验证 | 新增分批策略回归（大批量不再被 60 批拒绝、条数上限生效、超限错误含真实需求数与配置项）；修正两处依赖旧分批粒度的用例（`ExtractionRetryTest` 构造器补默认上限参数、跨批次去重用例片段体积调至约 2000 字恢复 ≥3 批场景） | `ExtractionBatchLimitTest.java`、`ExtractionRetryTest.java`、`ExtractionReviewFlowIntegrationTest.java` | 全量 `186/186`；`git diff --check` 通过 | 已完成 | 未降低覆盖、未跳过测试 |
| CHG-0044 | 2026-09-14 | RAG01 | 修复 | 前端引用定位 | 点击引用提示显示真实资料名、页/幻灯片/图片单元及已检索原文摘录，不再仅提示“按资料名称与页码查阅” | `AiChatPanel.vue` | `npm run type-check`、`npm run build` 通过 | 已完成 | 未增加前端依赖；仍不保存对话 |
| CHG-0045 | 2026-09-14 | RAG01 | 文档 | 规则同步 | 更新阶段 F 与项目状态，明确图谱是导航骨架、原文分段是问答事实依据以及本批只读边界 | `AGENTS.md`、`docs/第一版开发文档-Java-TailAdmin.md`、`docs/变更记录.md` | 文档与实现、测试结果逐项核对 | 已完成 | 后续恢复执行 `DB01`，未合并其实现 |
| CHG-0046 | 2026-09-14 | ZIP03 | 修复 | 资料抽取 | 附录/索引/目录页等「本批确实没有知识点」的批次被误判为失败（用户任务 #938：161 批跑完 160 批后，最后一批返回 `{"entities":[]}` → 解析器抛错 → 整任务 413/失败，前功尽弃）。现 `ExtractionPayloadParser` 放行显式空 `entities` 数组（仍拒绝缺少 entities 字段、非法 JSON、伪造 chunkId 等），并新增回归用例 | `ExtractionPayloadParser.java`、`ExtractionBatchLimitTest.java` | 定向 5/5；后端全量 `188/188` 失败 0 错误 0 | 已完成 | 未放宽证据与类型校验；无迁移、无新依赖 |
| CHG-0047 | 2026-09-14 | ZIP03 | 调整 | 资料抽取 | 批次改为并行执行（`BATCH_CONCURRENCY = 3`，结果仍按批序处理以保证候选键与进度可复现），整本教材等待时间约降为 1/3；系统提示词增加单批输出上限（≤24 实体/24 关系、定义 ≤80 字）以降低超长 JSON 截断概率 | `ExtractionService.java` | 定向与全量测试通过；并行化后跨批次去重用例改为「按内容决定答案」的顺序无关写法 | 已完成 | 未改变分批粒度与证据校验；失败仍为整体回滚（可重试） |
| CHG-0048 | 2026-09-14 | ZIP03 | 文档 | 说明 | 记录处理链路分工（本机：解压/文本提取/切分/分段/分批/校验/落库；云端模型：OCR、抽取、主题分类分库、AI 复审），并登记本轮真实测试结果 | `docs/变更记录.md` | 全量 `188/188`；`git diff --check` 通过 | 已完成 | 未把未执行内容写入 |
| CHG-0046 | 2026-09-14 | RAG01 | 修复 | 本地运行 | 定位截图仍返回旧答案的原因：8080 进程加载的是 10:47 的旧 JAR，而检索类仅编译在 classes 中；停止该已核验的旧后端进程后重新打包（Spring Boot 可执行 JAR）并启动新进程 | `target/backend-java-0.1.0.jar`、本地 8080 进程 | `mvnw.cmd package -B`：后端 `156/156`；经 `http://localhost:8080` 与 `http://localhost:5173` 代理分别实测 `POST /api/nodes/119/chat` 的“Java是什么”，均返回资料 #63、第 5 页、chunk #308 的真实引用 | 已完成 | 未改数据、未改接口；根 `start-dev.ps1` 因缺少本机 `.local/runtime.json` 未使用，已以当前后端目录直接启动 |
| CHG-0047 | 2026-09-14 | RAG01 | 修复 | 原文定位交互 | 原文定位改为可控卡片：右上角关闭；再次点击同一引用切换收起；点击其他引用切换至其资料、单元和摘录；定位文案增加片段 ID 便于区分同页多个分段 | `AiChatPanel.vue`、`docs/第一版开发文档-Java-TailAdmin.md` | `npm run build` 通过（含 type-check）；`git diff --check` 通过 | 已完成 | 不新增接口、依赖或持久化状态；面板关闭即丢弃定位内容 |
| CHG-0048 | 2026-09-14 | RAG01 | 修复 | 原文定位状态 | 节点切换监听先清空原文定位，再切换临时对话；「新对话」同样清空定位卡片，避免旧节点资料残留在新节点上下文 | `AiChatPanel.vue`、`docs/第一版开发文档-Java-TailAdmin.md` | `npm run type-check` 通过；`git diff --check` 通过 | 已完成 | 不改后端、接口、数据或持久化状态 |
| CHG-0049 | 2026-09-14 | RAG01 | 修复 | 快捷问答检索 | 将「通俗解释」「与相邻节点对比」「生成练习题」识别为承接当前节点的快捷问题，不再用其界面文案检索原文；节点名称/别名允许单字中文词，确保“栈”等节点可回退到自身原文证据 | `RetrievalService.java`、`RetrievalServiceTest.java`、`docs/第一版开发文档-Java-TailAdmin.md` | 定向 `RetrievalServiceTest` 4 项 + `ChatServiceAskTest` 11 项通过；全量 `mvnw test` 失败 0、错误 0；经 5173 代理实测节点 #119 的「通俗解释」返回资料 #63 第 66 页等真实引用 | 已完成 | 无迁移、无新依赖、无数据写入；显式技术词检索规则不变 |
| CHG-0050 | 2026-09-14 | RAG02 | 修复 | 全文问答检索 | 显式中文完整词组在全文分段中零命中时，回退为最多 8 个相邻二字片段检索；完整词组命中时不降级，排序仍以实际命中的词和资料关联度为准。PDF 全文继续保存在既有 `document_units`/`document_chunks`，图谱节点只作为导航与补充，不将每句话伪造成节点 | `RetrievalService.java`、`RetrievalServiceTest.java`、`docs/第一版开发文档-Java-TailAdmin.md` | 定向 `RetrievalServiceTest` 与 `ChatServiceAskTest` 通过；全量 `mvnw test` 退出码 0；重打包并重启 8080 后，经 5173 实测「线性结构和非线性结构的区别」返回资料 #63 第 20 页等 3 条真实引用，`insufficientEvidence=false`；同时核验资料 #63 第 5/10/20/30/40/50/60/66 页均有原文与 chunks | 已完成 | 无迁移、无依赖、无数据写入或重跑 PDF；查询仍只读已完成且在用资料 |
| CHG-0051 | 2026-09-14 | ING01 | 修复 | 摄取流水线 | 纯图片资料（ZIP 图片 / 扫描件）在缺少视觉模型通道时不再被误报为「文档中没有可抽取的文本」：单元状态区分 `OCR_SKIPPED`（无识别通道）与 `OCR_FAILED`（通道存在但调用失败或返回空文本）；零分段时按状态归因，给出 `DOCUMENT_OCR_REQUIRED` / `DOCUMENT_OCR_FAILED` 与可操作提示，保留原始文件与图片单元供配置模型后重试 | `IngestionPipeline.java`、`OcrGaps.java`、`ErrorCodes.java` | 实测上传纯图片 ZIP（夹具 `target/fixtures/sample-book.zip`，3 张中文页面图）触发任务 #751：修复前为 `DOCUMENT_NO_EXTRACTABLE_TEXT`「文档中没有可抽取的文本片段」；修复后重试返回 `DOCUMENT_OCR_REQUIRED`「该资料是纯图片或扫描内容，共 3 张图片需要识别…」，3 个单元保留为 `OCR_SKIPPED`；`IngestionPipeline.OcrRun` 记录真实失败原因 | 已完成 | 无迁移、无新依赖；重试入口 `POST /api/processing/jobs/{id}/retry` 复用既有流程，配置视觉模型后即可补全 |
| CHG-0052 | 2026-09-14 | ING01 | 修复 | 摄取流水线 | 视觉模型返回空文本时按识别失败处理（原实现标记 `OCR_OK` 但无文本，知识缺口被静默放过）；部分图片未识别时写入「本次入库的知识不完整」告警，并区分「未启用视觉能力模型」与「识别失败（附原因）」两种情况 | `IngestionPipeline.java`、`OcrGaps.java` | 定向 `OcrGapsTest` 7 项、`ExtractionReviewFlowIntegrationTest` 21 项（含 5 项新增归因用例）通过；全量 `mvnw test` `171/171` 失败 0 错误 0 | 已完成 | 告警写入 `ingestion_jobs.error_message`（截断至 490 字符），前端处理中心已展示该字段并提供重试 |
| CHG-0053 | 2026-09-14 | ING01 | 新增 | 测试 | 新增端到端回归：ZIP 内两张图片 → 模拟视觉模型转写 → 图片单元 `OCR_OK` 与原文分段 → 复审接受 → 真实 `commit` 单事务入库 → `RetrievalService` 命中图片原文并指回 `book-page-1.png` | `ImageIngestionToRetrievalIntegrationTest.java` | `mvnw test -Dtest=ImageIngestionToRetrievalIntegrationTest` 1/1 通过；断言含图片单元状态、图片分段数、图片原文成为节点证据、检索结果的 `unitType=image` 与 `sourceLocator` | 已完成 | 全程不调用真实模型（`OcrProvider`/`LlmClient` 均以 `@MockitoBean` 替代）；夹具库/资料/节点/文件在 `@AfterEach` 清理 |
| CHG-0054 | 2026-09-14 | ING01 | 修复 | 恢复入口 | 处理中心「提取知识」（`POST /api/processing/jobs/{id}/extract`）对纯图片资料给出与流水线一致的引导，不再只说「文档没有可抽取的文本片段」 | `ExtractionService.validateRecovery` | 经 8080 实测任务 #751 返回 422 `DOCUMENT_OCR_REQUIRED` 与同一条可操作提示 | 已完成 | 归因逻辑集中在 `OcrGaps`，两条入口共用同一文案，避免再次分叉 |
| CHG-0055 | 2026-09-14 | DB01 | 修复 | 配置 | 注册 V7 迁移：`V7__document_metadata.sql`（`document_metadata` / `document_tags` / `library_aliases`）此前未出现在任何 `schema-locations` 中，全新安装会缺表并在上传时写入 `document_metadata` 失败 | `application.yml`、`application-local.yml`、`application-local.yml.example` | 三处均补齐 V7 且脚本为 `CREATE TABLE IF NOT EXISTS` 幂等；重启后端后 V7 随启动执行成功（`target/backend-java-0.1.0.jar`，Hikari 连接正常、Tomcat 8080 起来） | 已完成 | 不改表结构、不迁移数据；已存在的开发库中 V7 表此前由手工执行建立，本次仅补配置 |
| CHG-0056 | 2026-09-14 | ING01 | 文档 | 规则同步 | 明确「知识载体与格式无关」与「图片未被识别不得静默丢失」两条规则：PDF 页 / 幻灯片 / ZIP 图片统一进入 `document_units` → `document_chunks` 作为问答语料，图谱只是导航骨架；OCR 单元状态与零分段错误码写入开发文档 §8.5 | `docs/第一版开发文档-Java-TailAdmin.md`、`AGENTS.md`、`docs/变更记录.md` | 文档与实现、错误码、实际上传验证逐项核对 | 已完成 | 未把未执行内容写入；`B01`–`B06` 仍未开始 |
| CHG-0057 | 2026-09-14 | ING01 | 删除 | 验证残留数据 | 逐条核验（ID=1024、名称「待 AI 识别 · sample-book.zip」、documentCount=1 / nodeCount=0 / edgeCount=0、资料 sha256 与本次上传一致且 status=FAILED）后，通过 `DELETE /api/libraries/1024` 删除本次验证产生的空测试库与失败资料 #979 | `knowledge_libraries` 1024、`documents` 979、`ingestion_jobs` 751、`document_units` / `document_metadata` / `auto_document_imports` 中该资料的行 | 删除后 `GET /api/libraries` 返回 9 个库（Java语言基础/数据结构与算法基础/线性表/栈与队列/递归与分治/树与二叉树/图/查找/排序，各 1 份资料）；数据库复查 `documents=0 / document_units=0 / ingestion_jobs=0 / auto_document_imports=0 / document_metadata=0` | 已完成 | 未使用 LIKE/REGEXP/范围 SQL；未删除任何正常知识库、资料与节点 |
| CHG-0058 | 2026-09-14 | ING01 | 修复 | 图片识别 | 修复 ZIP 图片永远无法识别：`ZipImageExtractor` 把 `image/png`、`image/jpeg` 当作 `OcrProvider` 的 `imageFormat` 传入（接口约定是裸格式名 png/jpeg/webp），拼接后得到 `data:image/image/png`，视觉模型一律返回「不支持的图片」。改为传裸格式名；并在 `VisionLlmOcrProvider` 增加 `normalizeFormat` 防御（剥掉 `image/` 前缀、jpg→jpeg），使同类调用错误不再静默失效 | `ZipImageExtractor.java`、`VisionLlmOcrProvider.java` | 修复前：真实上传纯图片 ZIP（临时库 #1138 + 资料 #1090）OCR 全部失败，`errorCode=DOCUMENT_OCR_FAILED`，原因含上游 400「You have uploaded an unsupported image…（支持 webp, png, jpeg）」；修复后重试同一任务：3 个图片单元全部 `OCR_OK`（`ocr_used=1`，转写 47/45/51 字中文原文）、产生 3 个分段、AI 复审自动入库 6 个节点（TCP/UDP/三次握手/SYN/SYN+ACK/ACK）、任务 COMPLETED 且 10 个候选；问答实测节点 #855「UDP 的开销和时延特点」返回 `insufficientEvidence=false` 与引用（`unitType=image`、`unitIndex=2`、`sourceLocator=book-page-2.png`、摘录为第 2 页原文） | 已完成 | 该缺陷自阶段 D 起存在，与是否配置视觉模型无关；PDF 扫描页路径原本传 `png` 故未暴露 |
| CHG-0059 | 2026-09-14 | ING01 | 修复 | 图片识别 | 视觉通道上游报错可读化：复用 `LlmClient.mapUpstreamError` 的错误码映射（401/403→`LLM_AUTH_FAILED`、5xx→`LLM_UNREACHABLE`、其余 4xx→`LLM_BAD_RESPONSE`）与 `LlmClient.redactApiKey` 打码，替换原先直接把上游原始 JSON 拼进消息；上游 4xx 且响应体提到 image 时补一句「该模型可能不接受图片输入，请确认勾选「视觉能力」的档案是真正的多模态模型」；响应校验改用 `LlmClient.extractContent`（同时获得 `finish_reason=length` 的截断提示） | `VisionLlmOcrProvider.java` | `mvnw test -Dtest=VisionLlmOcrProviderTest` 4 项通过；全量 `178/178` 失败 0 错误 0 | 已完成 | 不新增依赖；错误码字面值与文本通道一致，前端 LLM_* 文案映射同样适用 |
| CHG-0060 | 2026-09-14 | ING01 | 修复 | 错误文案 | 上游原因过长时只截断原因本身（≤300 字符、空白压成一行），保证「原文已保留、可在处理中心重试本任务」这句一定不会被 490 字符列宽挤掉 | `OcrGaps.java` | `OcrGapsTest` 新增 2 项（超长原因不丢可操作尾部、多行原因压平），该测试类 9 项全通过 | 已完成 | 仅改文案组装；错误码与状态机不变 |
| CHG-0061 | 2026-09-14 | ING01 | 新增 | 测试 | 新增 MIME/格式回归测试：`VisionLlmOcrProviderTest` 钉住 data URL 必须是 `data:image/png;base64,…`（含带前缀入参、jpg→jpeg、未知格式兜底）；`ZipImageExtractorTest` 钉住 ZIP 单元输出裸格式名与自然排序 | `VisionLlmOcrProviderTest.java`、`ZipImageExtractorTest.java` | 定向 5 项通过；后端全量 `178/178` | 已完成 | 不触发真实模型；全部为纯逻辑测试 |
| CHG-0062 | 2026-09-14 | ING01 | 删除 | 验证残留数据 | 真实图片联调的数据回滚：临时库 #1138「图片OCR联调临时库」、资料 #1090、任务 #840、候选、本次新建节点 #1013/#1014/#1015（SYN、SYN+ACK、ACK）、本次新增关系 #981~#984、本次写入的 7 行 `node_evidence` 与 `library_nodes` 关联，以及磁盘上的 ZIP 副本；**保留** 09-13 由 AI 搜索补全生成的 TCP 知识簇（节点 #854~#862、关系 #922~#929） | `knowledge_libraries` 1138、`documents` 1090、`ingestion_jobs` 840、`entity_candidates`/`relation_candidates` job 840、`knowledge_nodes` 1013~1015、`knowledge_edges` 981~984、`node_evidence`、`library_nodes`、`node_aliases`、磁盘 `storage/documents/3025d4a8-*.zip` | 单事务执行后逐项复查全部为 0；确认退出后 `GET /api/libraries` 9 个库、`documents` 1 份（#63）、`jobs` 1 个（#63）；TCP 簇 8 条关系与 9 个节点完整；全库无孤儿 `node_evidence`（`chunk_id` 失配 0 行） | 已完成 | 未删用户既有节点与关系；未使用 LIKE/REGEXP/范围 SQL；删除对象均为本次 ID 精确匹配 |
| CHG-0063 | 2026-09-14 | ING01 | 文档 | 规则同步 | 记录图片识别根因（MIME 前缀）与「ZIP 图片必须能被识别」的验收口径；开发文档 §8.5 的 `OcrProvider` 约定补充裸格式名要求 | `docs/第一版开发文档-Java-TailAdmin.md`、`AGENTS.md`、`docs/变更记录.md` | 文档与实现、真实联调结果逐项核对 | 已完成 | 未把未执行内容写入 |
| CHG-0064 | 2026-09-14 | CHAT02 | 调整 | AI 问答 | 回答策略改为两层（用户决策「举例是可以的」）：拒答只针对**主题无关**（问题与当前节点及原文分段都不相关），主题相关时**不得拒答**——资料写到的事实必须标 `[编号]`，资料没写到的部分由模型用自己的知识讲解、延伸、对比和举例；补充内容不得标注 `[编号]`、不得声称来自资料。替换原先「资料不足以回答就必须只返回固定拒答句、不得使用资料之外的知识」的一刀切规则 | `ChatService.buildSystemPrompt` | 新策略前老策略实测（节点 #128 递归、#119 栈）：`PHP是什么` 拒答 ✓、`栈的特点是什么` 正常答带引用 ✓、但 `递归和栈是什么关系？为什么栈可以实现递归` 与 `举一个生活中的例子说明栈` 均被拒答 ✗；新策略实测：`PHP是什么` 仍拒答（`insufficientEvidence=true`、0 引用）✓，另两问正常回答（3 条引用）且模型主动划界「下面的讲解来自我自己的知识补充，不属于上述资料内容」，例题（餐盘/浏览器后退/Ctrl+Z）与关系讲解（调用栈/后进先出匹配嵌套/显式栈改写）均给出 | 已完成 | 仅改提示词，未改检索、引用映射、接口或前端；`insufficientEvidence` 判定与固定拒答句不变 |
| CHG-0065 | 2026-09-14 | CHAT02 | 新增 | 测试 | 新增策略断言：提示词必须说明「只有主题无关才拒答」、明确允许并要求举例、显式禁止「因为资料里没有现成答案就拒绝回答」；同时钉住引用纪律（模型补充不得标 `[编号]`、不得声称来自资料、资料事实不得虚构编号） | `ChatServiceContextTest.java` | 定向 `ChatControllerTest` 7 + `ChatServiceAskTest` 11 + `ChatServiceContextTest` 7 = 25/25 通过；后端全量 `180/180` 失败 0 错误 0 | 已完成 | 纯静态断言，不触发真实模型 |
| CHG-0066 | 2026-09-14 | CHAT02 | 文档 | 规则同步 | 开发文档 §1 约束条与 §17.5 验收清单改为两层策略（主题无关才拒答；相关主题必须讲解且补充内容不挂引用），§7.3 说明同步 | `docs/第一版开发文档-Java-TailAdmin.md`、`AGENTS.md`、`docs/变更记录.md` | 文档与提示词、真实问答结果逐项核对 | 已完成 | 未把未执行内容写入 |
| CHG-0067 | 2026-09-14 | CHAT02 | 修复 | 前端提示文案 | 面板空态提示原为「AI 回答默认只依据知识库证据…不会编造答案」，与新策略不符（资料没覆盖的部分现在会由 AI 补充讲解），改为「以知识库资料为事实来源，资料内容会标注出处编号；资料没覆盖到的部分由 AI 补充讲解，与当前知识点无关时会提示资料不足」 | `AiChatPanel.vue` | `npm run type-check`、`npm run build` 通过（仅既有大 chunk 警告） | 已完成 | 纯文案，不改逻辑；未新增依赖 |
| CHG-0068 | 2026-09-14 | CP01 | 新增 | 数据库 | 新增抽取批次断点表 `extraction_batch_results`（批次指纹 + 该批 chunkId 顺序 + 校验后的规范化抽取结果），随 `ingestion_jobs` 级联删除；注册进三处 `schema-locations` | `V8__extraction_checkpoint.sql`；`application.yml`、`application-local.yml`、`application-local.yml.example` | 迁移已在开发库执行成功（`SHOW CREATE TABLE` 核对主键/唯一键/外键）；重启后端后 `/api/health` 返回 `database: UP`，启动无 SQL 报错 | 已完成 | 未新增依赖；建表为 `IF NOT EXISTS` 幂等，可用 `DROP TABLE extraction_batch_results` 回退（仅丢失断点数据，不影响候选/知识） |
| CHG-0069 | 2026-09-14 | CP01 | 新增 | 数据访问 | 新增 `ExtractionCheckpointStore`：批次指纹（片段条数+长度+内容的 SHA-256）、按批读写、按批/按任务清理、按索引删除多余批次 | `ExtractionCheckpointStore.java` | 由 `ExtractionCheckpointResumeIntegrationTest` 4 项用例以真实 MySQL 覆盖 | 已完成 | 指纹只由片段内容决定，不含 chunkId，故重新解析后仍可复用 |
| CHG-0070 | 2026-09-14 | CP01 | 调整 | AI 抽取 | `ExtractionService` 改为增量落库：每批解析校验通过后立即写入断点表（在工作线程内完成，与批序无关）；启动时按指纹复用已落库批次，只对缺失批次调用模型；进度以「已就绪批数」为起点；候选写入事务内清除断点数据；`validateRecovery(force=true)` 先作废断点数据 | `ExtractionService.java` | `ExtractionCheckpointResumeIntegrationTest` 4/4：失败后重试时第 1 批 0 次调用、第 2 批补 1 次；重新解析（chunkId 变化）仍复用且证据重映射到新编号；片段内容变化则该批重抽；force 作废后两批都重抽 | 已完成 | 未改候选表结构与状态机；批次失败仍不写候选，AI 复审与入库仍为同一事务 |
| CHG-0071 | 2026-09-14 | CP01 | 调整 | 处理中心 | `ProcessingService.retry` 增加续跑分支：解析产物完整（有片段且无 `NEEDS_OCR`/`OCR_SKIPPED`/`OCR_FAILED` 单元）时不再删除 `document_units`，直接从 AI 抽取阶段续跑；异步调度改到事务提交后执行，避免异步线程读到提交前的 `CANCELLED`/`FAILED` 而自我取消；任务 DTO 增加 `stagedBatches` | `ProcessingService.java` | `ExtractionCheckpointResumeIntegrationTest.retryResumesFromStagedBatchesWithoutReextractingThem`：重试后单元数/片段数不变（未重解析），任务达 `AWAITING_REVIEW`，断点数据清零；`ExtractionReviewFlowIntegrationTest` 21 项回归全绿 | 已完成 | 图片单元未识别时仍走原「清理重跑」路径，保证「配置视觉模型后重试」继续重新 OCR |
| CHG-0072 | 2026-09-14 | CP01 | 调整 | AI 复审 | `AiReviewService` 复审前读取各候选已有的复审结论，同一模型且已判定的候选不再送审，只复审缺失项；进度与单位改为按候选条数（原为按批数） | `AiReviewService.java` | `AiReviewFlowIntegrationTest.resumeReusesStoredAuditsAndDoesNotCallModelAgain`：入库事务失败后重跑，模型调用数保持 1（不增加）且任务 `COMPLETED`、入库 2 节点；该类 7 项全绿 | 已完成 | 换模型后不复用旧结论（复审结论按模型名匹配）；候选被重抽（行删除重建）时旧结论自然失效 |
| CHG-0073 | 2026-09-14 | CP01 | 新增 | 前端 | 处理中心任务卡：`FAILED`/`CANCELLED` 且存在断点批次时提示「已保留 N 批抽取结果，点『重试任务』将从断点继续，这些批次不会再调用模型」；计数单位区分抽取（批）/复审（条）/解析（单元） | `ProcessingTable.vue`、`types.ts` | `npx eslint`（改动两文件）无输出；`npm run type-check`、`npm run build` 通过（仅既有大 chunk 警告） | 已完成 | 未改按钮布局与交互；无新依赖 |
| CHG-0074 | 2026-09-14 | CP01 | 新增 | 测试 | 新增 `ExtractionCheckpointResumeIntegrationTest`（真实 MySQL + 模拟模型 + 17 片段两批夹具）：失败后增量落库、重试续跑不重调模型、重解析后复用与证据重映射、指纹变化重抽、force 作废；`AiReviewFlowIntegrationTest` 增加复审续跑用例 | `ExtractionCheckpointResumeIntegrationTest.java`、`AiReviewFlowIntegrationTest.java` | 定向 57/57；后端全量 `193/193` 失败 0 错误 0（较上批 188 增加 5 项） | 已完成 | 测试夹具自建自清（任务删除即级联清除断点行），不动用户真实任务 |
| CHG-0075 | 2026-09-14 | CP01 | 文档 | 规则同步 | 新增「增量落库与断点续跑」说明文档（机制、指纹语义、重试/重抽行为、运维排查、回退方式），并在项目说明中登记本批次 | `docs/增量落库与断点续跑.md`、`AGENTS.md`、`docs/变更记录.md` | 文档内容与代码、测试结果逐项核对；未执行内容未写入 | 已完成 | 仅文档，无业务代码变化 |
| CHG-0076 | 2026-09-14 | CP01 | 删除 | 全项目 | 本批次没有删除文件、接口、数据表、业务数据、功能或依赖；未删除任何既有断点/候选数据（此前不存在断点表） | 当前工作区 CP01 差异 | `git status --short` 核对：新增 8 个文件（迁移/存储/测试/说明文档），修改 8 个文件，无删除项 | 已完成 | 回退方式：删除 `V8` 迁移与断点表、还原上述文件即可 |

> 类型只使用：`新增`、`修复`、`删除`、`调整`、`文档`。同一工作同时包含多种类型时拆成多行记录。

## 分批计划

| 批次 | 目标 | 本批包含 | 明确不包含 | 完成验证 | 状态 |
|---|---|---|---|---|---|
| R01 | 修复资料标签与请求边界 | 启用请求体校验；标签清洗后判空；满额时允许同名标签幂等/转人工；支持删除含 `/` 的标签；限制 AI 主题标签长度 | 不改页面布局；不改数据库结构；不处理时间和可信度文案 | 新增边界测试；后端相关测试与全量测试；前端 type-check、build | 已完成 |
| R02 | 修复资料属性展示准确性 | 后端返回真实元数据更新时间；哈希改为“已记录”语义；人工校对可取消 | 不新增验证历史；不重新读取文件计算哈希 | 后端全量 146/146；前端 type-check、build；UI 实测取消/重勾双向保存 | 已完成 |
| R02A | 收尾资料人工校对取消 | 前端双向提交人工校对状态；保存前快照编辑对象，避免异步期间取消导致空引用；对齐 `updatedAt` 可空类型 | 不改后端接口/数据库；不改布局；不开始 B01 | 后端全量 146/146；前端 type-check、build；UI 实测取消/重勾 | 已完成 |
| SEC01 | 修复 3D 图谱 Tooltip 存储型 XSS | 组件内新增 `escapeHtml`，节点名/类型标签/定义/关系名插入 Tooltip 前全部转义；截断在转义前完成 | 不改后端与 API；不删 Tooltip；不处理存量包体积与 ESLint 存量问题 | eslint 单文件、type-check、build 通过；真实组件函数抽测 7/7；渲染边界对照验证（修复前 img+onerror 执行、修复后 0 个 img/script 且未执行） | 已完成 |
| CHAT01 | 节点 AI 助手改为无历史临时模式 | 面板默认折叠、打开即空对话、连续追问仅用临时历史；后端改单条无状态问答路由并删除会话持久化路径；切换节点/关闭面板中断在途请求 | 不建表不迁移；不清理既有会话数据；不改视觉布局；不开始 B01 | 后端 152/152；前端 eslint/type-check/build 通过；浏览器 8 项验收；chat_sessions/chat_messages 行数 6/15 前后不变 | 已完成 |
| LIB01 | 测试夹具隔离与知识库展开互斥 | 测试夹具改事务回滚；精确删除残留空测试库；「资料」与「查看知识」整页互斥 | 不改库结构；不删正常业务数据；不做其他审查项 | 定向 11/11×2 + 全量 152/152；前端 lint/type-check/build；浏览器 A–F；git diff --check | 已完成 |
| ZIP01 | 放行 ZIP 内的 PDF 条目 | ZIP 允许 PDF，按页生成 page 单元并在定位中带上 ZIP 内文件名；文案同步 | 不放行其他类型；不改库结构；不改 OCR 通道 | 定向 13/13、全量 183/183；端到端上传含 PDF 的 ZIP 通过；前端 lint/type-check/build | 已完成 |
| ZIP02 | 抽取批次上限适配整本教材 | 单批预算 12000 字/16 片段；批次上限可配置 `kg.extraction.max-batches`（默认 400）；超限错误给出真实需求数与配置项 | 不新增依赖、不改库结构、不放宽证据校验 | 定向 3/3；后端全量 186/186；`git diff --check` | 已完成 |
| ZIP03 | 抽取空批次放行与批次并行 | 放行「本批无实体」；批次 3 路并行；提示词限制单批输出量 | 不放宽证据/类型校验；不改分批粒度；不引入新依赖 | 定向 5/5；后端全量 188/188；git diff --check | 已完成 |
| RAG01 | 原文分段驱动的问答检索 | 只读检索已完成且在用资料的 chunks；显式问题优先全文关键词，代词问题回退节点证据；回答编号映射为真实引用 | 不重跑 PDF；不写会话；不新增迁移/依赖；不扩充图谱节点 | 后端关键词/引用单测与全量 156/156；前端 type-check/build；8080/5173 真实问答调用 | 已完成 |
| RAG02 | 中文 PDF 全文检索回退 | 显式完整中文词组零命中时，用相邻二字片段在既有全文 chunks 中检索；保留完整词组优先和图谱证据回退 | 不新增向量库、迁移、依赖或图谱节点；不重跑资料 | 定向/全量后端测试；重启后真实问答带引用；抽查多页原文与 chunks | 已完成 |
| ING01 | 图片资料的知识不因缺视觉模型而误报丢失，且真实可被识别 | 区分 `OCR_SKIPPED` / `OCR_FAILED`；零分段按状态归因给出 `DOCUMENT_OCR_REQUIRED` / `DOCUMENT_OCR_FAILED` 与可操作提示；OCR 返回空文本按失败处理；部分缺口写「知识不完整」告警；两条入口共用同一归因；修复 ZIP 图片 `imageFormat` 前缀导致视觉模型永远拒收；视觉通道错误码/打码对齐文本通道 | 不引入本地 OCR 依赖；不改表结构、不加迁移；不自动启用或修改用户的模型档案；不改前端 | 纯图片 ZIP 真实上传、修复前后对照与重试实测；真实视觉模型转写出中文原文并可被问答引用；`OcrGapsTest` 9 项 + `VisionLlmOcrProviderTest` 4 项 + `ZipImageExtractorTest` 1 项 + `ExtractionReviewFlowIntegrationTest` 21 项 + ZIP→OCR→分段→入库→检索端到端用例；后端全量 178/178 | 已完成 |
| DB01 | 注册 V7 迁移 | 在 `application.yml`、`application-local.yml`、`application-local.yml.example` 的 `schema-locations` 中补上 `V7__document_metadata.sql` | 不改表结构、不迁移数据、不改脚本内容 | 重启后端确认 V7 随启动幂等执行且服务正常 | 已完成 |
| CHAT02 | 问答回答策略改为两层 | 拒答只针对主题无关；主题相关时必须讲解、延伸、举例，资料事实标引用、模型补充不挂引用且不声称来自资料 | 不改检索、引用映射、接口与前端；不引入模型之外的知识源；不改 `insufficientEvidence` 判定契约 | 老/新策略真实模型对照实测（PHP 拒答、栈正常答、递归与栈关系与生活例子由拒答转为可答）；定向 25/25；后端全量 180/180 | 已完成 |

| B01 | 补齐节点现有元数据 UI | 节点详情显示 `updatedAt`；编辑已有别名；补齐前端类型和请求载荷 | 不建表；不做标签；不改 AI、验证、状态流程 | 前端 type-check、build；手工检查保存/取消/错误态 | 待实施 |
| B02 | 建立标签数据基础 | 新增标签、节点标签、知识库标签表；后端读写、归一化、校验和测试 | 不改前端；不接搜索；不调用模型 | 数据库迁移检查；后端标签相关测试及全量测试 | 待实施 |
| B03 | 增加标签界面 | 节点与知识库显示、添加、删除标签；知识库本地标签筛选 | 不改全局搜索；不改 AI 自动整理 | 前端 type-check、build；桌面/窄屏/明暗主题检查 | 待实施 |
| B04 | 标签参与本地搜索 | 节点标签加入联想排序并显示命中标签 | 不改变 AI 搜索显式触发规则 | 搜索排序回归；后端全量测试；前端构建 | 待实施 |
| B05 | 增加可信度摘要 | 候选映射到最终节点；展示 AI 复审、证据、资料数和哈希记录状态 | 不新增验证历史表；不重新读取文件计算哈希 | 五种验证状态及事务回滚测试；前端验证详情检查 | 待实施 |
| B06 | AI 自动补知识库标签 | 在现有自动主题分类调用中附带标签并幂等保存 | 不增加模型调用；不自动复制标签到节点 | 无标签响应兼容、非法标签、重试幂等及全量回归 | 待实施 |

## 当前执行边界

- 用户指定的 `CP01`（增量落库 + 断点续跑）已完成：抽取批次按批落库并支持断点续跑，AI 复审复用既有结论，处理中心展示已保留批数。
- 下一批从 `B01` 开始（补齐节点现有元数据 UI）；`B02`–`B06` 依次进行，未经用户指定不跳跃。
- `CHAT02` 连带发现 `REV-021`（回答以纯文本渲染，Markdown 符号原样显示）。修复它要么引入 Markdown 渲染依赖（§0.7 需先征得同意），要么自写无依赖的轻量渲染（需用户确认支持范围）；两者都未实施，等用户指定。
- 视觉识别现已在真实模型上跑通：用户在「系统设置 → 模型档案」把 `deepseek` 档案标记为视觉能力后，ZIP 图片可真实转写中文并进入问答引用。**用户已决定（2026-09-14）：暂不开发本地免费 OCR 兜底**，因此图片识别继续只走云端视觉模型通道；后续如需离线能力，须先按 §0.7 征求用户同意再新增依赖。
- 遗留审查项 `REV-020`（删除资料不清理磁盘文件，现存 9 个孤儿文件）未安排批次。
- **现场证据（2026-09-14，CP01 起因）**：任务 #938《textbook.zip》真实跑完 161/161 批抽取（16:16–17:15，约 1 小时模型调用）后，在 AI 主题分类阶段报 `LLM_BAD_RESPONSE`「资料包含超过 12 个独立主题」而失败；由于候选只在分类成功后写入，这 161 批成果全部作废，且重试会再花一小时重跑。CP01 之后同样的失败只损失分类那一步（断点数据保留 161 批），重试只补缺失批次；该任务本次**未**重跑（会真实消耗模型额度且分类限制是确定性的），保留原状由用户决定是否拆分资料。
- 每个批次完成后，在上方“变更记录”逐项登记实际的新增、修复、删除和测试结果。
- 若实施中发现必须扩大范围，先把原因和影响记录为 `受阻`，再向用户说明，不自行合并多个批次。

## 审查发现

| 编号 | 严重度 | 发现 | 影响 | 处理批次 | 状态 |
|---|---|---|---|---|---|
| REV-001 | 高 | 请求 record 有校验注解，但控制器未使用 `@Valid`；`humanChecked=null` 可能在拆箱时触发 500 | 非法请求未稳定返回 400 | R01 | 已修复 |
| REV-002 | 高 | 标签在控制字符替换前判空，清洗后未再次 `strip`/判空 | 可能写入空白标签 | R01 | 已修复 |
| REV-003 | 中 | 资料已有 20 个标签时，重复添加现有标签也先被数量上限拦截 | AI 同名标签无法幂等升级为人工标签 | R01 | 已修复 |
| REV-004 | 中 | 删除标签使用路径变量，合法标签含 `/` 时 URL 路径无法可靠表达 | 部分可添加标签无法从界面删除 | R01 | 已修复 |
| REV-005 | 高 | AI 主题名允许 200 字，但资料标签列仅 100 字，直接写入可能导致整次分类事务回滚 | 不可信模型输出可令自动整理失败 | R01 | 已修复 |
| REV-006 | 中 | 后端未返回 `document_metadata.updated_at`，前端用创建时间兜底 | 编辑后仍显示旧更新时间 | R02 | 已修复 |
| REV-007 | 中 | 上传时只计算并保存 SHA-256，却标记/展示为“哈希已校验” | 可信度表达强于实际证据 | R02 | 已修复 |
| REV-008 | 高 | 人工校对复选框仍仅在勾选时提交，取消勾选不会写回 | 页面显示可取消，但已校对状态实际无法撤销 | R02A | 已修复 |
| REV-009 | 低 | 后端在无元数据时返回 `updatedAt: null`，前端却声明为 `updatedAt?: string` | TypeScript 合同未表达接口的 null 值 | R02A | 已修复 |
| REV-010 | 中 | `saveDocEditor` 在第一个 `await` 后继续解引用响应式 `docEditor.value`，而取消按钮仍可将其设为 null | 保存过程中取消弹窗可能触发运行时错误，且服务端可能已部分保存 | R02A | 已修复 |
| REV-011 | 高 | 纯图片资料（ZIP 图片 / 扫描件）在没有 `vision=true` 模型档案时全部单元被标记 `NO_OCR`，分段为 0 后抛出 `DOCUMENT_NO_EXTRACTABLE_TEXT`「文档中没有可抽取的文本片段」 | 用户的图片知识 100% 未入库，且错误文案把「缺少识别通道」说成「文件没有内容」，用户无法判断该做什么 | ING01 | 已修复 |
| REV-012 | 中 | 原计划写给用户的告警「部分图片单元未识别：未配置具备视觉能力的模型」位于零分段失败之后，纯图片资料永远走不到该分支，提示形同死代码 | 缺少视觉模型时没有任何可操作提示 | ING01 | 已修复 |
| REV-013 | 中 | `NO_OCR` 同时表示「没有识别通道」「识别调用抛异常」「该单元本来无文字」三种情况 | 无法区分配置缺失与识别故障，重试引导可能给错方向 | ING01 | 已修复 |
| REV-014 | 中 | 视觉模型返回空文本时仍写入 `status='OCR_OK'` | 单元显示为已识别却对问答零贡献，知识缺口静默通过 | ING01 | 已修复 |
| REV-015 | 中 | 「提取知识」恢复入口（`ExtractionService.validateRecovery`）对同一情况只报「文档没有可抽取的文本片段」 | 同一种问题在两条入口给出两种误导文案 | ING01 | 已修复 |
| REV-016 | 高 | `V7__document_metadata.sql` 未列入任何 `schema-locations`（`application.yml`、`application-local.yml`、`.example` 三处均缺） | 全新安装缺 `document_metadata` / `document_tags` / `library_aliases`，上传资料写入元数据即失败；当前开发库仅因手工执行过脚本而正常 | DB01 | 已修复 |
| REV-017 | 高 | `ZipImageExtractor` 把 `image/png`、`image/jpeg` 当作 `OcrProvider` 的 `imageFormat` 传入，而接口约定是裸格式名（png/jpeg/webp）；`VisionLlmOcrProvider` 直接拼接得到 `data:image/image/png` | **ZIP 图片自阶段 D 起永远无法被识别**，视觉模型一律返回「You have uploaded an unsupported image」；与是否配置视觉模型无关。PDF 扫描页路径恰好传 `png`，所以问题长期只暴露在 ZIP 上 | ING01 | 已修复 |
| REV-018 | 中 | 视觉通道未复用 `LlmClient` 的错误码映射与密钥打码，直接把上游原始 JSON 响应体拼进用户可见消息 | 401/404/5xx 在两条模型通道上给出不同错误码；用户看到的是英文原始报文而非可操作原因 | ING01 | 已修复 |
| REV-019 | 低 | `OcrGaps` 先组装完整消息再整体截断到 490 字符 | 上游原因较长时会把「原文已保留、可重试」这句可操作提示截掉 | ING01 | 已修复 |
| REV-020 | 低 | 删除资料只删数据库记录：磁盘 `storage/documents/` 下存在 9 个不被任何 `documents` 行引用的孤儿文件（本次核验时仅 #63 的 `b935c068-*.pdf` 属于在库资料，其余为阶段 D/E 验收与早期上传遗留） | 长期占用磁盘；无法从界面判断哪些文件是垃圾 | 未安排 | 待处理 |
| REV-021 | 中 | `ChatMessage.vue` 以 `{{ message.content }}` 纯文本插值渲染回答（`whitespace-pre-wrap`，无 Markdown 支持），而模型稳定输出 `##` 标题、`**粗体**`、``` 代码块与表格 | 回答里的 Markdown 语法会以原始符号显示（用户截图已可见 `**示例一…**` 与 ```java 围栏），分层讲解与举例越长越难读；CHAT02 鼓励举例后该问题更明显 | 未安排 | 待处理 |
| REV-022 | 高 | `ExtractionService` 的批次数百次模型调用全部只在「所有批次成功」后一次性落库（候选表），且候选写入还排在 AI 主题分类之后；任何一批失败、取消、进程中断，或分类/复审环节失败，都让此前全部分批调用作废 | 整本教材量级资料（实测任务 #938：161 批、约 1 小时）失败一次即从头重跑，重复消耗模型额度与时间；失败越靠后损失越大 | CP01 | 已修复 |
| REV-023 | 中 | `AiReviewService` 虽在每批复审后落 `aiReview` 结论，但重跑时 `loadItems` 重新装载全部候选并整批送审，既有结论完全不参与判断 | 复审/入库阶段失败后重试会把已判定的候选再送模型一遍，同样按资料规模线性浪费 | CP01 | 已修复 |

## 单项记录模板

复制下行并替换内容：

| CHG-XXXX | YYYY-MM-DD | BXX | 新增/修复/删除/调整/文档 | 模块 | 实际完成内容 | 文件、接口、表或数据范围 | 执行的命令与结果 | 已完成/受阻/已取消 | 风险、备份或恢复方式 |
